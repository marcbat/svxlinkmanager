@using SvxlinkManager.Models
@using System.Globalization
@using Microsoft.Extensions.Localization

@page "/Channel/Edit/{Id}"
@inherits SvxlinkManager.Pages.Channels.EditBase<SvxlinkChannel>

@inject IStringLocalizer<Manage> Loc

<h3>
  @if (Channel.IsDefault)
  {<span class="oi oi-home" aria-hidden="true" data-toggle="tooltip" data-placement="top" title="Le salon @Channel.Name est actuellement le salon par défaut."></span>}
  Editer @Channel.Name
</h3>

<AddEditForm Channel="@Channel" HandleValidSubmit="@(()=>HandleValidSubmit("Channel"))" SubmitTitle="@SubmitTitle" CanEditType="@CanEditType" />